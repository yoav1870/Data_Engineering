FROM bitnami/spark:3.5.1

# WORKDIR is already set by Bitnami, optional
WORKDIR /opt/bitnami/spark/app

# Optional: copy app code if needed
COPY . .

# COMMENT OUT the jar copy line since it's not there yet
# COPY jars/iceberg-spark-runtime-3.5_2.12-1.4.3.jar /opt/bitnami/spark/jars/
